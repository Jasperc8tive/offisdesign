# Storefront (Next.js) production env.
# These are baked into the client bundle at build time — never put secrets here.

NEXT_PUBLIC_API_URL=https://api.offisdesign.com
NEXT_PUBLIC_WEB_URL=https://offisdesign.com
NEXT_PUBLIC_STRIPE_PUBLISHABLE_KEY=pk_live_REPLACE
NEXT_PUBLIC_MEDIA_HOSTNAME=cdn.offisdesign.com
